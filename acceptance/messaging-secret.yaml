apiVersion: v1
kind: Secret
metadata:
  name: messaging-secret
type: Opaque
stringData:
  # JWT Secret - REPLACE WITH ACTUAL SECRET
  APP_JWT_SECRET: "CHANGE_ME_R+afECUXmYIOfn+zNwjaUNYHOJAmFjqQ8LQdiht6MQA="
  
  # Email Configuration - REPLACE WITH ACTUAL CREDENTIALS
  SPRING_MAIL_HOST: "smtp.gmail.com"
  SPRING_MAIL_PORT: "587"
  SPRING_MAIL_USERNAME: "your-email@example.com"
  SPRING_MAIL_PASSWORD: "your-app-password"
  
  # Database password (if not using CloudNativePG auto-generated)
  # DB_PASS: "your-db-password"

# NOTE: This file should be encrypted with SOPS before committing!
# Run: sops -e -i acceptance/messaging-secret.yaml
