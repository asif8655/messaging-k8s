apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: messaging-staging
  namespace: flux-system
spec:
  interval: 5m
  path: ./staging
  prune: true
  sourceRef:
    kind: GitRepository
    name: messaging-k8s
  targetNamespace: messaging-staging
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: messaging-deployment
      namespace: messaging-staging
