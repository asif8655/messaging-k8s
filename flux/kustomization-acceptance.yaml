apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: messaging-acceptance
  namespace: flux-system
spec:
  interval: 5m
  path: ./acceptance
  prune: true
  sourceRef:
    kind: GitRepository
    name: messaging-k8s
  targetNamespace: messaging-acceptance
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: messaging-deployment
      namespace: messaging-acceptance
